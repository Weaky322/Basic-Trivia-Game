THIS IS FAR FROM FINISHED



This little trivia game is how I am learning the C language. It currently only works on Linux systems.

Compile with ``gcc -o [output name] [triviagame.c]``.

Run with ``./[output name]``.
